

<div class="testimonial-add">
	<p id="heading1"> Add new testimonial </p> <br>
	<center >
	<!--<%= link_to "New Testimonial", new_testimonial_path, method: :get,:class => "btn btn-blue btn-lg" %>-->
	<label for="modal-1">
    <div class="modal-trigger btn btn-blue btn-lg">Add Testimonial</div>
  	</label>

	</center>
	<ul>
	<% @testimonials.each do |t| %>
	<li> <h3> <%= t.message %> </h3> 
	<%= link_to 'edit', edit_testimonial_path(t) %> <br>
	<%= link_to 'delete', t, method: :delete, data: {confirm: "Are you sure?"} %></li> 
	<% end %>
	</ul>
</div>

<div class="modal">
 
  <input class="modal-state" id="modal-1" type="checkbox" />
  <div class="modal-fade-screen">
    <div class="modal-inner">
      <div class="modal-close" for="modal-1"></div>
      <h1>Modal Title</h1>
		<p>here the form will come to add the <%= render('form') %>
    </div>
  </div>
</div>
